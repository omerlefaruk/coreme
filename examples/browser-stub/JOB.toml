name = "browser-stub"
version = "0.1.0"
entry = "main.py"

[proof]
offline = "pytest -q"

[runtime]
timeout_sec = 60

[secrets]
names = ["SITE_USER", "SITE_PASSWORD"]

[inputs.headless]
type = "string"
default = "1"

[inputs.mode]
type = "string"
default = "work"
