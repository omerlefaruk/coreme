name = "koala-popup"
version = "0.1.0"
entry = "main.py"

[proof]
offline = "pytest -q"

[runtime]
timeout_sec = 60

[inputs.query]
type = "string"
required = false
default = "koala"

[inputs.message]
type = "string"
required = false
default = "a"
